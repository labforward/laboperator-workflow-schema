---
$schema: http://json-schema.org/draft-07/schema#
$id: /laboperator/workflow_step_template_schema/1.0.1
type: object
title: Workflow Step Template Schema
description: |
  A workflow step template consists of one or multiple substeps and can be used across multiple different workflow templates.
required:
  - schema_version
  - info
allOf:
  - $ref: '#/definitions/stepAttributes'
  - type: object
    properties:
      schema_version:
        $ref: '#/definitions/schemaVersion'
      info:
        $ref: '#/definitions/info'
      secrets:
        $ref: '#/definitions/secrets'

