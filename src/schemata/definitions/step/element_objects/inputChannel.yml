---
type: array
title: Input Channels
description: |
  Input Channels
items:
  oneOf:
    - $ref: '#/definitions/memberName' # channels field
    - type: object
      required:
        - device
      properties:
        device:
          $ref: '#/definitions/memberName' # device field
        channels:
          type: array
          items:
            $ref: '#/definitions/memberName' # channel names
